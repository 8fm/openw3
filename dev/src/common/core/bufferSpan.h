/**
* Copyright © 2014 CD Projekt Red. All Rights Reserved.
*/

#pragma once
#ifndef BUFFER_SPAN_H_
#define BUFFER_SPAN_H_

class CBufferSpan
{
public:
	//////////////////////////////////////////////////////////////////////////
	// Constructors
	//////////////////////////////////////////////////////////////////////////
	CBufferSpan( const Uint32 start, const Uint32 end );
	CBufferSpan( const CBufferSpan& other );
	CBufferSpan( CBufferSpan&& other );

	//////////////////////////////////////////////////////////////////////////
	// Destructor
	//////////////////////////////////////////////////////////////////////////
	~CBufferSpan();
	
	//////////////////////////////////////////////////////////////////////////
	// Operators
	//////////////////////////////////////////////////////////////////////////
	CBufferSpan& operator=( const CBufferSpan& other );
	CBufferSpan& operator=( CBufferSpan&& other );

	//////////////////////////////////////////////////////////////////////////
	// Public Methods
	//////////////////////////////////////////////////////////////////////////
	RED_INLINE const Uint32 GetStart() const
	{
		return m_start;
	}

	RED_INLINE const Uint32 GetEnd() const
	{
		return m_end;
	}

	RED_INLINE const Int32 GetDelta() const
	{
		return m_end - m_start;
	}

private:
	Uint32 m_start;
	Uint32 m_end;
};

#endif // BUFFER_SPAN_H_