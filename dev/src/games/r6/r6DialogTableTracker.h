#pragma once

#include "build.h"


//------------------------------------------------------------------------------------------------------------------
// Keeps track and memorizes the state of the dialog in the table
//------------------------------------------------------------------------------------------------------------------
class CR6DialogTableTracker : public CObject
{	
	DECLARE_ENGINE_CLASS( CR6DialogTableTracker, CObject, 0 );

private:

public:

};


BEGIN_CLASS_RTTI( CR6DialogTableTracker )
	PARENT_CLASS( CObject )	
	END_CLASS_RTTI()